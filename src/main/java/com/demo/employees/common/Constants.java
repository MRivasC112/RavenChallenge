package com.demo.employees.common;

public class Constants {
    private Constants() {

    }

    // Maximum allowed length for all employee name fields (first, middle, paternal, maternal).
    public static final int NAME_MAX_LENGTH = 50;

    // Date format pattern used for date-of-birth input/output (dd-MM-yyyy).
    public static final String DATE_FORMAT_PATTERN = "dd-MM-yyyy";
}
